/* style.qss
   UI 样式集中管理：方便维护、方便换主题
   -------------------------------------------------
   说明：
   - 牌名 QLabel：objectName = "CardNameLabel"
   - 数量 QLabel：objectName = "CardCountLabel"
   - depleted 状态：dynamicProperty depleted=true/false
     通过 QSS 属性选择器匹配：
     QLabel#CardNameLabel[depleted="true"] { ... }
*/

/* 全局默认：统一基础字体 */
QWidget {
    font-family: "Microsoft YaHei", "PingFang SC", "Noto Sans CJK SC";
    font-size: 14px;
}


/* 牌名：默认样式 */
QLabel#CardNameLabel {
    font-size: 14px;
    font-weight: 600;
    padding: 2px 6px;
}

/* 牌名 depleted：变灰 */
QLabel#CardNameLabel[depleted="true"] {
    font-size: 14px;
    font-weight: 600;
    color: #999;
    padding: 2px 6px;
}

/* 数量：默认样式 */
QLabel#CardCountLabel {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
}

/* 数量等于4时显示为淡红色 */
QLabel#CardCountLabel[count="4"] {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    color: #ff6666;
}

/* 数量 depleted：变灰 */
QLabel#CardCountLabel[depleted="true"] {
    font-size: 14px;
    padding: 1px 4px;
    border-radius: 6px;
    color: #999;
}
